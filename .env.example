# Panel 3 wariantów graficznych u góry strony (true/false).
# W DEV, gdy zmienna nie jest ustawiona, panel i tak się pokazuje.
VITE_SHOW_DEV_PANEL=true

# Ścieżka bazowa. Na FTP w katalogu głównym zostaw "/".
# Na GitHub Pages w repo np. "/NazwaRepo/"
VITE_BASE=/
